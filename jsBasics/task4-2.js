const length = (Math.random() * 20).toFixed(2); // updated due to  comment 'use const'
console.log('Random length of rectangle is ', length);
const width = (Math.random() * 20).toFixed(2); // updated due to  comment 'use const'
console.log('Random width of rectangle is ', width);
const rectangleArea = length * width; // updated due to  comment 'use const'
console.log('The calculated area of the rectangle is ', rectangleArea.toFixed(2));
